# Backend API URL
PUBLIC_API_URL=http://localhost:3001/api

# Astro DB (Turso) - Only needed for production/remote builds
# Get from: turso db show <db-name> --url
# ASTRO_DB_REMOTE_URL=libsql://your-database.turso.io

# Turso auth token - Only needed for production/remote builds
# Get from: turso db tokens create <db-name>
# ASTRO_DB_APP_TOKEN=your-token-here
